# Todo Frontend (React + Vite)

A simple, beginner-friendly Todo app frontend. No backend, no API calls —
all data lives in React state and resets on page refresh.

## Run locally

```bash
npm install
npm run dev
```

Then open the URL Vite prints (usually http://localhost:5173).

## Build for production

```bash
npm run build
npm run preview
```
